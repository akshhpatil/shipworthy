# Benchmark results contain generated projects with node_modules
# Only commit score JSON files and logs, not the full project directories
results/*/
!results/.gitkeep
!results/*-score.json
!results/*-scores.json
!results/*-log*.txt
